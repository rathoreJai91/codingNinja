package DataStructures.PriorityQueue;

import java.util.PriorityQueue;

public class inbuiltPQ {
    
    public static void main(String[] args) {
        //int [] arr = {1,3,2,5,4,10,8,11};
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        pq.poll();
        pq.add(0);
        pq.peek();
        pq.remove();
    }
}
